services:
  bot:
    build: .
    container_name: tarot-bot
    env_file:
      - .env
    environment:
      DB_PATH: /data/tarot.db
    volumes:
      - tarot-data:/data
    restart: unless-stopped

volumes:
  tarot-data:
